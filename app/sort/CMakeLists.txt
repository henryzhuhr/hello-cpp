include_directories(${PROJECT_SOURCE_DIR}/include)

# set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin/sort)
# message(" sort BINARY DIR:" ${EXECUTABLE_OUTPUT_PATH})

add_executable(sort sort.cpp)
